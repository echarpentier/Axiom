package fr.pfgen.axiom.shared;

public class AxiomUtils {
	
	/*public static Map<String,String> getCriteriaAsMap(String criteria){
		Map<String,String> critMap = new HashMap<String, String>();
		criteria = criteria.replace("{", "");
		criteria = criteria.replace("}", "");
		String keyVal[] = criteria.split(",\\s");
		for (int i = 0; i < keyVal.length; i++) {
			String kv[] = keyVal[i].split("=");
			critMap.put(kv[0], kv[1]);
		}
		return critMap;
	}*/
}
